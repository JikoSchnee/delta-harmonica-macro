# 用户路径英文对照与路径价值说明

本文档解释管理后台「使用分析 / 高频使用路径」中出现的英文术语，并说明每一步对产品的价值。
内容以当前项目实际埋点和统计逻辑为准，便于后续看数据时不需要猜英文含义。

## 先看结论：什么是用户路径

「用户路径」就是同一个匿名会话中，用户依次完成过哪些动作。例如：

```text
访问页面 → 选择曲库入口 → 载入曲目 → 开始试听 → 前往导出 → 完成宏导出
```

它回答的是：用户从哪里进入、先做什么、在哪一步停下、有没有最终获得宏文件。

当前系统为每个浏览器会话生成匿名 `session` 标识，只记录允许列表中的产品事件。为了让「谱子操作量」显示具体曲谱，会记录曲谱名称和作者；不会上传谱子正文、文件名、搜索词、IP 或剪贴板内容。

## `heartbeat` 是什么意思

| 英文 | 中文 | 实际含义 | 触发方式 | 路径价值 |
| --- | --- | --- | --- | --- |
| `heartbeat` | 心跳 / 活跃信号 | 页面仍处于前台，用户仍可能在使用 | 页面可见时每 60 秒一次；页面从后台回到前台时再记录一次 | 判断当前会话是否仍活跃、估算在线人数和活跃时长，避免把“打开网页后就离开”的会话误认为持续使用 |

它不是“用户完成了一步业务操作”，也不是登录心跳，更不是向用户发送消息。可以把它理解成：

> “这个页面现在还开着，而且用户可能还在这里。”

因此，查看转化路径时不应把 `heartbeat` 当成“用户下一步”。它主要服务于活跃访客统计和页面状态判断。

## 后台界面标题对照

| 英文 | 中文 | 说明 | 对应价值 |
| --- | --- | --- | --- |
| `ANALYTICS.EXE` | 数据分析控制台 | 后台分析页面的复古界面标题 | 统一展示访问、操作、导出和路径数据 |
| `PRIVATE CONSOLE` | 私有控制台 | 只有管理者可查看的后台区域 | 保护内部数据，避免把详细行为数据公开 |
| `CONVERSION FUNNEL` | 转化漏斗 / 使用漏斗 | 按阶段统计有多少匿名会话到达 | 找到用户在哪一步大量流失 |
| `ENTRY SOURCES / ACTIONS` | 进入来源 / 用户操作 | 汇总用户从哪里来，以及做过哪些动作 | 判断搜索、社交、外链等来源质量，并确定最常用功能 |
| `TOP JOURNEYS` | 高频使用路径 | 把同一会话里的连续事件拼成路径，并展示最常见的前几条 | 找到真实主流程，优化高频路径，减少不必要步骤 |
| `SCORE OPERATIONS` | 曲谱操作量 | 按曲谱名称和作者展示载入、编辑和导出；内部仍保留 ID 用于区分同名曲谱 | 判断哪些曲谱最有用、哪些曲谱促进了实际导出 |
| `MY LIBRARY` | 我的曲库 | 用户本地或账号曲谱管理区域 | 支持用户保存、复用和再次编辑，提升回访价值 |
| `COMMUNITY SCORE` | 社区曲谱 | 可被其他用户使用的共享曲谱 | 形成“投稿 → 被使用 → 再投稿”的内容循环 |

## 用户路径事件对照

下面的英文是数据中的事件名。后台已经有部分中文显示名，但这里补充了触发时机和路径价值。

| 事件名 | 中文含义 | 什么时候记录 | 主要回答的问题 | 路径价值 |
| --- | --- | --- | --- | --- |
| `page_view` | 访问页面 | 页面初始化时记录一次 | 有多少会话真正打开了工具？ | 计算访问量和漏斗起点 |
| `directory_selected` | 选择入口 | 用户点击曲库、MIDI 导入或手动输入入口 | 用户通常从哪里开始？ | 比较三种入口，决定首页和入口布局 |
| `library_search` | 搜索曲库 | 用户在曲库搜索框输入内容后触发 | 用户找什么歌？是否找不到想要的歌？ | 指导曲库补充、搜索体验和推荐内容 |
| `song_loaded` | 载入曲目 | 用户载入内置曲谱或社区曲谱 | 哪些曲谱被真正使用？ | 衡量曲库内容价值，发现热门曲谱 |
| `input_mode_selected` | 切换输入模式 | 用户选择简谱、录制、精确或三角洲键盘模式 | 用户偏好哪种制作方式？ | 决定默认模式、教程重点和编辑器优先级 |
| `midi_import_opened` | 打开 MIDI 导入 | 用户打开 MIDI 文件选择流程 | 有多少人使用 MIDI 作为输入？ | 评估高级导入功能的使用规模 |
| `midi_selection_applied` | 应用 MIDI 选择并生成谱子 | 用户选择音轨或音符后生成可编辑谱子 | MIDI 导入是否成功转成可用曲谱？ | 判断导入功能是否真正产生结果，而不只是被打开 |
| `score_imported` | 导入谱子 | 用户导入 `.deltamusic` 等曲谱文件 | 用户是否在复用或交换曲谱？ | 衡量文件分享、迁移和再编辑价值 |
| `preview_started` | 开始试听 | 用户点击试听 | 用户是否在导出前检查旋律？ | 判断试听是否是导出前的关键校验环节 |
| `macro_section_opened` | 前往宏导出区 | 用户打开或进入宏导出区域 | 用户是否从“编辑”走到了“使用”？ | 是从工具操作转向最终产出的重要信号 |
| `export_mode_selected` | 选择导出板块 | 用户切换通用导出或特殊驱动导出板块 | 用户使用哪类驱动？ | 决定驱动适配、说明文档和导出入口排序 |
| `macro_downloaded` | 下载宏文件 | 用户下载 Lua、Synapse 3/4 XML 或 ROG 文件 | 用户有没有拿到最终可用文件？ | 最核心的业务结果，衡量工具是否完成任务 |
| `lua_copied` | 复制 Lua | 用户将 Lua 内容复制到剪贴板 | 用户是否采用复制方式而非下载？ | 补充“完成导出”的判断，优化复制和下载体验 |
| `score_downloaded` | 下载曲谱文件 | 用户下载 `.deltamusic` | 用户是否保存或分享自己的曲谱？ | 衡量曲谱资产沉淀和跨会话复用 |
| `score_edit_opened` | 打开曲谱编辑 | 用户打开曲目编辑窗口 | 用户是否需要修改现有曲谱？ | 识别编辑需求和曲谱质量问题 |
| `score_edit_saved` | 保存曲谱编辑 | 用户保存修改后的曲谱 | 修改是否完成并形成新资产？ | 衡量编辑功能是否产生有效结果，而不是只被打开 |
| `community_upload_succeeded` | 成功投稿到曲库 | 投稿接口成功接收曲谱 | 用户是否愿意贡献内容？ | 扩大曲库供给，形成社区增长循环 |
| `tour_started` | 打开教程 | 用户打开新手教程 | 用户在哪里需要帮助？ | 发现理解门槛，降低首次使用失败率 |
| `heartbeat` | 活跃心跳 | 页面前台每 60 秒一次，或回到前台时一次 | 用户是否仍停留在页面？ | 估算活跃会话和在线人数；不代表业务完成 |

## 漏斗中的六个阶段

后台当前把事件汇总为以下六个阶段。每个阶段按“出现过该事件的匿名会话数”统计：

| 阶段 | 包含事件 | 用户语言 | 业务价值 |
| --- | --- | --- | --- |
| 访问页面 | `page_view` | 用户打开了工具 | 计算总访问规模 |
| 选择入口 | `directory_selected` | 用户开始选择一种使用方式 | 判断首页是否成功把用户引导到功能入口 |
| 获得曲谱 | `song_loaded`、`midi_selection_applied`、`score_imported` | 用户已经有一份可以继续处理的曲谱 | 代表从“浏览工具”进入“实际创作/处理” |
| 开始试听 | `preview_started` | 用户检查旋律和节奏 | 说明用户开始验证结果质量 |
| 前往导出 | `macro_section_opened` | 用户准备把曲谱转换成宏 | 接近最终任务目标 |
| 完成宏导出 | `macro_downloaded`、`lua_copied` | 用户获得了宏文件或 Lua 内容 | 最接近产品核心价值的完成事件 |

### 阅读漏斗时的注意事项

这里的漏斗不是严格的“每一步都按时间顺序完成”的实验漏斗。后台代码统计的是：一个会话是否出现过某阶段的事件，然后计算会话数。因此：

- 同一用户可以先编辑、后试听，也可以直接从已有曲谱导出。
- `heartbeat` 不属于漏斗阶段。
- “完成宏导出”不一定是下载文件，也可能是复制 Lua。
- 漏斗人数减少只能说明到达该阶段的会话更少，不能单独证明具体原因；需要结合高频路径和事件次数一起看。

## 高频路径怎么读

`TOP JOURNEYS` 会把一次会话中的连续事件拼起来，相邻的同名事件会合并，最多展示前 7 个节点。例如：

```text
访问 → 入口:library → 选曲:community → 试听 → 宏导出区 → 下载:lua
```

它不是用户看到的页面 URL，而是产品行为路径。常见路径及价值如下：

| 典型路径 | 用户意图 | 路径价值 | 可采取的优化 |
| --- | --- | --- | --- |
| 曲库入口 → 载入曲目 → 试听 → 宏导出区 → 下载宏 | 直接使用已有曲谱 | 最短、最接近核心目标的成功路径 | 保持入口清晰，默认推荐热门且可导出的曲谱 |
| MIDI 导入 → 生成 MIDI 谱 → 试听 → 下载宏 | 把外部 MIDI 转成游戏宏 | 验证高级能力是否能带来最终导出 | 提升多音轨选择、错误提示和导入后默认试听体验 |
| 手动输入 → 切换输入模式 → 试听 → 导出 | 自己制作或精细调整曲谱 | 体现编辑器的创作价值 | 优化输入示例、快捷键和时值提示 |
| 搜索曲库 → 载入曲目 → 试听 | 目标明确但先寻找内容 | 反映曲库是否满足需求 | 根据搜索词补充曲谱，改善搜索无结果提示 |
| 打开教程 → 选择入口 → 继续使用 | 初次使用或遇到疑问 | 反映产品学习成本 | 把教程放到最容易卡住的步骤旁边 |
| 载入曲谱 → 打开编辑 → 保存编辑 → 导出 | 复用已有内容并加工 | 体现曲谱资产的长期价值 | 保留编辑结果，减少重复输入 |
| 导出曲谱 → 投稿曲库 → 他人载入 | 用户贡献内容并被复用 | 形成社区内容供给闭环 | 降低投稿门槛，同时保留审核质量 |

## 建议优先关注的指标

如果只看少量指标，建议按以下顺序：

1. `page_view → 获得曲谱 → 完成宏导出`：判断工具是否真正帮用户完成任务。
2. `获得曲谱 → 开始试听`：判断用户是否会验证结果，以及试听是否足够明显。
3. `前往导出 → 完成宏导出`：判断导出区是否容易理解、文件是否容易成功生成。
4. `library_search` 与 `song_loaded`：判断曲库是否满足用户需求。
5. `midi_import_opened → midi_selection_applied`：判断 MIDI 功能是“被尝试”还是“真正成功”。
6. `heartbeat`：只用于活跃度和在线状态，不要用它代替转化指标。

## 一句话速查

| 英文 | 最简记忆 |
| --- | --- |
| `page_view` | 打开网页 |
| `directory_selected` | 选入口 |
| `song_loaded` | 拿到曲谱 |
| `preview_started` | 开始试听 |
| `macro_section_opened` | 准备导出 |
| `macro_downloaded` | 下载宏，任务基本完成 |
| `heartbeat` | 页面还活跃，不代表完成任务 |
| `funnel` | 看每个阶段剩多少人 |
| `journey` | 看用户实际怎么走 |
| `session` | 一次匿名使用会话 |
| `event` | 一次被记录的用户行为 |
