# Harmonica Deck

将《三角洲行动》口琴数字简谱转换为鼠标宏文件，并在浏览器中试听、编辑和分享谱子。

开发、部署与曲库维护说明请见 [develop.md](develop.md)。

后台使用分析中的英文术语、用户路径和各路径价值说明请见 [USER_PATH_GLOSSARY.md](USER_PATH_GLOSSARY.md)。

## 在线使用

- 正式站点：[三角洲口琴演奏家](https://jiko-official.top/delta/)
- 备用站点（可能需要网络工具）：[GitHub Pages](https://jikoschnee.github.io/delta-harmonica-macro/)
- 网页版本：`2.1.2`（录制助手启动时会自动检查更新）
- QQ 群：`1102489399`（[入群二维码](assets/qq-group-qr.jpeg)）

## 快速开始

1. 在「曲库」选择一首歌，或在编辑器贴入自己的简谱。
2. 设置 BPM，并点击「试听整段」检查旋律与节奏。
3. 在「导出为宏」选择你的鼠标驱动，下载对应文件。
4. 先用曲库中的《口琴按键映射测试》确认鼠标按键映射，再使用正式曲目。

谱子和 MIDI 文件只会在浏览器本地处理；本工具不会连接游戏、鼠标软件或其他外部程序。

## 输入谱子

编辑器提供四种可相互切换的视图，切换时会校验谱面内容：

- **简谱模式**：最适合照着数字简谱录入。裸音默认一拍，例如 `5_ #4_ 5_ 0_`。
- **录制模式**：按住实体键盘或页面琴键实时录入时值。
- **精确模式**：用 `前缀音符/拍数` 表示，例如 `1/1 M2/0.5 R3/0.5`。
- **三角洲键盘模式**：逐行显示键盘、鼠标修饰键和毫秒时值，便于手动填入其他宏工具。

简谱中：`0` 是休止，`_` / `__` 分别为半拍 / 四分之一拍，`.` / `..` 分别增加二分之一 / 四分之三原时值；`#4`、`♭7` 表示升降音，`1'`、`,1` 表示高低八度。`|` 可用作小节线，`~` 可连接同一音高。

## 导入 MIDI

点击「导入 MIDI」并选择 `.mid` 或 `.midi` 文件。工具会跳过打击乐轨，优先挑选主旋律，并读取文件开头的 BPM、拍号和调号。导入后建议试听；复杂多轨曲目可使用「精确选中」逐个保留需要的音符。

「流畅演奏」会把相邻音之间很短的空拍并入前一个音，适合原始 MIDI 有大量短断音的情况；关闭后会保留原始断音。

## 试听与曲库

「试听整段」会在浏览器中合成口琴音色、展示时间线，并高亮当前音符，用于检查旋律与节奏。试听音色是近似预览，不是录音采样。

曲库中的《口琴按键映射测试》包含自然音、低音、半音、高音及其组合。首次使用新的鼠标映射时，建议导出并逐组确认后再播放正式曲目。

## 导出与驱动

- **Logitech G HUB**：导出 Lua 脚本。请按页面提示填写触发与停止设置。
- **Razer Synapse 3 / 4**：两种 XML 格式不互通；请导入对应版本，并在驱动内绑定触发方式。
- **迈从 MCHOSE**：导出与迈从样例一致的 `.json` 宏文件；若当前驱动版本导入失败，请先录制一个最小宏进行格式对照。
- **ROG Armoury Crate**：导入 `.gmac` 后，在 Macro 页面绑定到支持宏功能的设备按键。
- **录制助手**：下载 Windows 宏录制助手后，先在目标宏软件中打开录制，再在助手中点击开始按钮并等待录制完毕。录制期间不要进行操作，鼠标焦点应始终停留在助手上；完成后停止并保存宏，删除开头点击开始按钮产生的一次鼠标按下/放开。助手启动时会检查更新，发现新版后可确认下载、校验并覆盖更新。GitHub 下载速度更快，无法下载时可使用服务器下载。

如果目标宏软件或迈从录制无法捕获助手输入，请在助手的「输入兼容模式」中逐项测试。当前助手提供以下 8 种 Windows 内置模拟输入组合：

- 默认兼容：`keybd_event` 扫描码 + `mouse_event`
- 兼容虚拟键：`keybd_event` 虚拟键 + `mouse_event`
- 标准 SendInput：`SendInput` 虚拟键 + `SendInput` 鼠标
- 迈从兼容：`SendInput` 扫描码 + `SendInput` 鼠标
- 混合 A：`keybd_event` 扫描码 + `SendInput` 鼠标
- 混合 B：`keybd_event` 虚拟键 + `SendInput` 鼠标
- 混合 C：`SendInput` 虚拟键 + `mouse_event`
- 混合 D：`SendInput` 扫描码 + `mouse_event`

建议使用短谱按顺序测试；每次切换方案后重新点击「开始录制」。这些方案都是 Windows 标准用户态输入 API，若目标程序使用驱动级拦截，仍可能无法捕获。

不同驱动版本和设备对宏事件的支持可能不同。若导入失败，请从该驱动导出一个最小单键宏作对照。

## 分享与投稿

「分享 .deltamusic」会导出可再次导入的谱子文件。若正式站点显示「上传到曲库」，登录后可直接投稿；请只上传你拥有分享权的原创或已获授权谱面。

也可以将 `.deltamusic` 文件发送到 QQ 群，或 Fork 本仓库并创建 Pull Request。建议文件名使用 `歌名-作者-你的昵称.deltamusic`。

## 使用提醒

请自行确认目标游戏、服务器与鼠标软件的规则是否允许使用宏。因使用宏产生的风险与后果由使用者自行承担。

## 共同开发

欢迎其他作者参与开发、修复问题、改进导入导出能力、完善文档和投稿曲谱。请先阅读 [CONTRIBUTING.md](CONTRIBUTING.md)，再通过 Issue 或 Pull Request 提交想法和代码。

提交 Pull Request 是项目推荐的协作方式：这样可以让修改进入同一个公开仓库，方便审查、讨论和后续维护。贡献者仍保留自己贡献内容的版权；提交贡献即表示同意将该贡献按 [PolyForm Noncommercial License 1.0.0](LICENSE) 授权给本项目及其用户。

## 许可证

本项目源代码采用 [PolyForm Noncommercial License 1.0.0](LICENSE) 授权，仅允许非商业目的使用、修改和分发。该协议不是 OSI 认可的标准开源许可证；如需商业授权，请先联系作者。

- `data/community-scores/` 中的曲谱文件：按各自作者或权利人的授权使用，不自动受本协议授权。
- `assets/` 中的第三方图片、教程截图、二维码等素材：除文件另有说明外，不授予超出原权利人授权范围的权利。
- 仓库中包含的第三方依赖、字体和其他外部素材：继续适用其各自的许可证或授权条款。

如果你要复制、修改或分发本项目的代码，请先阅读并遵守完整的 [LICENSE](LICENSE) 条款。
